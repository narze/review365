{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "bun install",
  "buildCommand": "bun run --filter web build && rm -rf build && cp -a apps/web/build build",
  "outputDirectory": "build"
}
